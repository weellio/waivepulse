# WAIvePulse dependencies
# Install into your heartlib virtualenv:
#   pip install -r requirements.txt
#
# NOTE: PyTorch and heartlib are NOT listed here — they must already be
# installed in your virtualenv as part of the HeartMuLa setup.

# Core server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Metadata embedding
mutagen>=1.47.0

# Model download
huggingface_hub>=0.20.0

# BPM / key detection (optional — safe to remove if you don't need it)
librosa>=0.10.0
numpy>=1.24.0

# Stem separation — Studio page (optional — safe to remove if you don't need it)
demucs>=4.0.0

# AI content watermarking — PAWS Act / C2PA compliance (optional)
# AudioSeal: neural audio watermark, survives re-encoding
audioseal>=0.1.0
torchaudio>=2.0.0          # usually already installed with PyTorch
# C2PA: cryptographic provenance manifest embedded in MP3
c2pa>=0.5.0
cryptography>=41.0.0
